<%=
  cert = nil

  respond_to?(:if_link) && if_link("etcd") do |link|
    if link.p("etcd.peer_require_ssl")
      cert = link.p("etcd.peer_ca_cert")
    end
  end.else do
    if p("etcd.peer_require_ssl")
      cert = p("etcd.peer_ca_cert")
    end
  end

  cert
%>
