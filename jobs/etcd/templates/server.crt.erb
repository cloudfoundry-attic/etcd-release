<%=
  cert = nil

  respond_to?(:if_link) && if_link("etcd") do |link|
    if link.p("etcd.require_ssl")
      cert = link.p("etcd.server_cert")
    end
  end.else do
    if p("etcd.require_ssl")
      cert = p("etcd.server_cert")
    end
  end

  cert
%>
